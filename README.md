Smart Budget
=============
[![CI](https://img.shields.io/github/actions/workflow/status/jasokolowska/smart-budget/ci.yml?branch=main)](https://github.com/jasokolowska/smart-budget/actions)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Java](https://img.shields.io/badge/Java-21-blue.svg)](https://www.oracle.com/java/technologies/javase/jdk21-archive-downloads.html)

Brief description
-----------------
Smart Budget is a modular personal budget management application. The backend is implemented in Java 21 using Spring Boot and Spring Modulith. The frontend (client/) is a placeholder and can be implemented as a web app (Angular/Vue/React) or a mobile app (React Native/Flutter).

Status
------
Proof of concept / early development. The backend compiles and includes tests that generate module documentation (PlantUML).

Table of contents
-----------------
- [Quick start](#quick-start)
- [Repository structure](#repository-structure)
- [Backend (server)](#backend-server)
- [Documentation](#documentation)
- [Contributing](#contributing)
- [CI and containerization](#ci-and-containerization)
- [Local git hooks](#local-git-hooks)
- [License](#license)
- [Contact](#contact)

Quick start
-----------
Requirements:
- Java 21 (project configured using Gradle toolchain)
- Gradle (use the included wrapper)
- Optional: Docker

Run the backend (from the repository root or `server/`):
- Windows: `cd server && gradlew.bat bootRun`
- *nix: `cd server && ./gradlew bootRun`

Build:
- Windows: `cd server && gradlew.bat build`
- *nix: `cd server && ./gradlew build`

Run tests:
- Windows: `cd server && gradlew.bat test`
- *nix: `cd server && ./gradlew test`

Repository structure
--------------------
- `client/` — frontend (currently empty)
- `docs/` — product and technical documentation (PRD, user stories, requirements, iteration plan, architecture)
- `server/` — backend (Spring Boot + Modulith)
- `CONTRIBUTING.md` — contribution guidelines
- `LICENSE` — project license

Backend (server)
-----------------
Detailed instructions and information about the backend are available in `server/README.md`. In short:
- The project uses Java 21 and the Gradle wrapper.
- Spring Modulith generates module diagrams in `build/spring-modulith-docs/`.
- The built JAR is placed in `build/libs` after a successful build.

Documentation
-------------
Product and technical documents are located in `docs/`. Module diagrams are generated by tests and saved to `server/build/spring-modulith-docs/`.

Contributing
------------
Contribution workflow and guidelines are described in `CONTRIBUTING.md`. Briefly: open an issue → fork → feature branch → pull request. Pull requests should pass local tests and include a description of changes.

CI and containerization
-----------------------
A GitHub Actions workflow was added at `.github/workflows/ci.yml` to run Gradle tests on push and pull requests. To enable the CI badge above, replace OWNER/REPO in the badge URL with your repository coordinates.

A Dockerfile is not included; adding a simple Dockerfile based on `openjdk:21` is recommended for container images.

Local git hooks
--------------------------
This repository includes local Git hook scripts (under `.githooks/`) to run checks before pushing:
- `.githooks/pre-push` — POSIX shell script for Unix/macOS
- `.githooks/pre-push.bat` — Batch script for Windows

What they do:
- If `client/package.json` exists, run Prettier and ESLint checks in the `client/` folder.
- If Kotlin files are present under `server/`, run `ktlint` (or a Gradle `ktlintCheck` task on Windows if configured).

Install hooks (recommended)

- Unix / macOS:

  1. Make the installer executable and run it from repo root:

     ```sh
     chmod +x scripts/install-hooks.sh
     ./scripts/install-hooks.sh
     ```

  2. The script sets `git config core.hooksPath .githooks` so Git will execute the scripts found there.

- Windows (cmd.exe / PowerShell):

  1. Run the installer script from the repo root:

     ```cmd
     scripts\install-hooks.bat
     ```

  2. The script sets `core.hooksPath` to `.githooks`.

Manual installation
- Set hooks path manually:

  ```sh
  git config core.hooksPath .githooks
  ```

- To uninstall/remove the hooks path:

  ```sh
  git config --unset core.hooksPath
  ```

Notes and troubleshooting
- Make sure Node.js (for `npx`) is available to run Prettier/ESLint checks.
- The Windows hook tries to run `gradlew.bat ktlintCheck` if Kotlin files are found; configure the Gradle ktlint plugin if you want automatic checks.
- If you prefer to fail pushes instead of blocking them locally, change the hook scripts accordingly.

License
-------
This project is licensed under the MIT License — see the `LICENSE` file.

Contact
-------
To contribute, open an issue or submit a pull request in this repository.

