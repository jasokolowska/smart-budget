---
version: "3.9"
services:
  postgres:
    image: "postgres:14.2-alpine"
    ports:
    - "5432:5432"
    environment:
    - "POSTGRES_PASSWORD=S3cret"
    - "POSTGRES_USER=smart_budget"
    - "POSTGRES_DB=smart_budget_db"
